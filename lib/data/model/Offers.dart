class Offers {
  String offerTitle;
  String offerDescription;
  String iconUrl;
  String offerRating;
  String offerCategory;
  String validityInDays;

  Offers(this.offerTitle, this.offerDescription, this.iconUrl, this.offerRating,
      this.offerCategory, this.validityInDays);
}
